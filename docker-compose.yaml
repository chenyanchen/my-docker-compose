version: "3.9"

services:
  mysql:
    image: mysql:8.0.27
    platform: linux/amd64
    container_name: mysql
    volumes:
      - $HOME/data/mysql:/var/lib/mysql
    networks:
      backend:
        aliases:
          - mysql
    ports:
      - "3306:3306"
      - "33060:33060"
    environment:
      - MYSQL_ROOT_PASSWORD=999

  redis:
    image: redis:6.2.6
    container_name: redis
    volumes:
      - $HOME/data/redis:/data
    networks:
      backend:
        aliases:
          - redis
    ports:
      - "6379:6379"

  postgres:
    image: postgres:14.2
    container_name: postgres
    volumes:
      - $HOME/data/postgres:/var/lib/postgresql/data
    networks:
      backend:
        aliases:
          - postgres
    ports:
      - "5432:5432"
    environment:
      - "POSTGRES_PASSWORD=999"
  #      - "PGDATA=/var/lib/postgresql/data"

  elasticsearch:
    image: elasticsearch:8.0.0
    container_name: elasticsearch
    volumes:
      - $HOME/data/elasticsearch:/usr/share/elasticsearch/data
    networks:
      backend:
        aliases:
          - elasticsearch
    ports:
      - "9200:9200"
      - "9300:9300"
    environment:
      - "discovery.type=single-node"
      #- "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      #- "xpack.security.enabled=true"

  kibana:
    image: kibana:8.0.0
    container_name: kibana
    networks:
      backend:
        aliases:
          - kibana
    ports:
      - "5601:5601"
      #volumes:
      #- $HOME/data/kibana/config/kibana.yml:/usr/share/kibana/config/kibana.yml
    environment: #https://www.elastic.co/guide/en/kibana/current/docker.html#docker-defaults
      #SERVER_NAME: kibana.example.org
      - "ELASTICSEARCH_HOSTS=http://elasticsearch:9200"
    depends_on:
      - elasticsearch

  grafana:
    image: grafana/grafana:8.4.2
    container_name: grafana
    networks:
      backend:
        aliases:
          - grafana
    ports:
      - "3000:3000"
    volumes:
      - $HOME/data/grafana:/var/lib/grafana

  prometheus:
    image: prom/prometheus:v2.33.4
    container_name: prometheus
    networks:
      backend:
        aliases:
          - prometheus
    ports:
      - "9090:9090"
    volumes:
      - $HOME/data/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - $HOME/data/prometheus:/prometheus

networks:
  backend:
    name: backend
